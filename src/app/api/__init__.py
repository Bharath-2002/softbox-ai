"""HTTP surface.

Routers, request/response schemas, auth dependencies, and the mapping from
domain exceptions to ``application/problem+json``. No business logic.

May import: agents, features, services, entities, shared.
"""
