"""Domain model: entities, value objects, enums, domain events, invariants.

Pure Python. Zero SQLAlchemy, zero FastAPI, zero I/O. SQLAlchemy tables and
their imperative mappings live in ``infrastructure.persistence`` (D7).

May import: shared.
"""
